{
  "name": "@emilvooo/opencode-usage",
  "version": "1.0.0",
  "description": "Simple CLI to view OpenCode provider usage (Claude, Codex) at a glance",
  "type": "module",
  "bin": {
    "opencode-usage": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Emilvooo/opencode-usage.git"
  },
  "keywords": ["opencode", "claude", "codex", "openai", "anthropic", "usage", "cli"],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.9.3"
  }
}
